# ORIEL IDE Windows Alpha

This directory contains the first native Windows installer alpha for ORIEL IDE. The GitHub Actions workflow builds an NSIS `.exe` on `windows-latest`. The alpha opens real folders and text files and delegates Check, Build, Run and Test commands to the existing ORIEL CLI.
