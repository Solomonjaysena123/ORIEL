# ORIEL 0.6.0 release artifacts

This directory contains the Python wheel built from the preserved ORIEL 0.6.0 source snapshot.

A version-specific VS Code extension snapshot was not preserved for this release, so no VSIX has been fabricated.
