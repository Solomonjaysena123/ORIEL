# ORIEL 0.8.1 release artifacts

This directory contains:

- `oriel_language-0.8.1-py3-none-any.whl` — Python wheel built from the active 0.8.1 source.
- `oriel-language-support-0.8.1.vsix` — packaged VS Code extension built from the active extension source.
