# ORIEL v0.8.2 release checklist

- [x] Version metadata is `0.8.2`.
- [x] MIT license is retained.
- [x] QA framework, documentation, automated tests, and CI workflow are included.
- [ ] Run `python -m pytest` in an environment with the development dependency installed.
- [ ] Review `git diff` in the canonical ORIEL checkout.
- [ ] Commit with `git commit -m "Add ORIEL v0.8.2 QA Automation Framework"`.
- [ ] Create the annotated tag with `git tag -a v0.8.2 -m "ORIEL v0.8.2"`.
- [ ] Push the commit and tag only after explicit owner confirmation.

This package was prepared from the clean v0.8.1 release snapshot. That snapshot did not contain `.git` history, so no commit or tag was created here; apply/copy these changes into the canonical repository before performing the final four steps.
