# ORIEL 0.1.0 Historical Milestone

Initial console interpreter prototype with early lexer, parser, AST, runtime, CLI commands and VS Code syntax support.

This folder is a historical milestone record. The active implementation is maintained in the repository root and is currently version 0.7.3.
