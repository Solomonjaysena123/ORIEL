# ORIEL 0.6

Application-services release with a SQLite repository ORM, declarative validators, PBKDF2 password hashing, signed authentication tokens, environment configuration loading, and CRUD scaffolding.
